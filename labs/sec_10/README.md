Thực hiện lần lượt các lệnh sau

```bash
docker compose up
docker network ls
docker ps -a
docker compose stop
docker ps -a
docker compose start
docker ps -a
docker compose down
docker ps -a
docker network ls
```